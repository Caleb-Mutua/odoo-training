<odoo>
    <record id="view_users_form_inherit_properties" model="ir.ui.view">
        <field name="name">res.users.form.inherit.properties</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <!-- Add property_ids field at the end of the first notebook -->
            <xpath expr="//notebook" position="inside">
                <page string="My Properties">
                    <field name="property_ids" widget="one2many_list" readonly="1"/>
                </page>
            </xpath>
        </field>
    </record>
</odoo>